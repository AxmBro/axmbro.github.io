import React from "react";
import "./ScreenSection.css";

function ScreenSection({ children }) {
  return (
    <div className="ScreenSection">
      {children}
    </div>
  );
}

export default ScreenSection;
